# Build outputs -- never committed historically in this repo either
# (confirmed via git log --all before adding this), regenerated by
# build.bat from source every time.
x64/
*.elf
*.oelf
*.prx
*.pkg
eboot.bin
pkg.gp4
